import { Button } from '@qijenchen/design-system'

export default function App() {
  return (
    <main className="min-h-screen bg-canvas text-foreground p-8">
      <h1 className="text-h2 mb-6">App Template</h1>
      <p className="text-body text-fg-secondary mb-4">
        替換此處 with your product UI。Consume <code>@qijenchen/design-system</code> components,never modify DS source.
      </p>
      <Button variant="primary">Get started</Button>
    </main>
  )
}
